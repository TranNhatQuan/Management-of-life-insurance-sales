<!DOCTYPE html>
<html>

<head>
  <title>LIFE</title>
  <link rel="icon" href="../../icon/2764.png" type="image/png">
  <style>
    h3 {
      font-size: smaller;
      opacity: 0.5;
    }

    .form-group {
      margin: 10px;
    }

    #idUser {
      outline: none;
      border: none;

    }

    button {
      display: inline-block;
      padding: 5px;
      background-color: #3f87a6;
      border: none;
      color: #fff;
      font-weight: bold;

      border-radius: 4px;
      text-decoration: none;
      cursor: pointer;
    }

    input {

      border: 1px solid #ccc;
      border-radius: 4px;
    }

    select {
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    .container {
      border-radius: 10px;
    }
  </style>

</head>

<body>



  <h2>Sửa thông tin khách hàng</h2>
  <form action="/user/edit/<%= user.idUser %>?url=/user/listUser" method="post">
    <div class="form-group">
      <label for="idUser">ID:</label>
      <input type="text" id="idUser" name="iduser" value="<%= user.idUser %>" readonly>
    </div>
    <div class="form-group">
      <label for="name">Tên:</label>
      <input type="text" id="name" name="name" value="<%= user.name %>" required required pattern="^\S.{4,48}\S$"
        title="Tên phải có độ dài từ 6 đến 50 ký tự.">
    </div>
    <div class="form-group">
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" value="<%= user.mail %>" required>
    </div>
    <div class="form-group">
      <label for="phone">Số điện thoại:</label>
      <input type="tel" id="phone" name="phone" value="<%= user.phone %>" required pattern="0\d{9,10}"
        title="Số điện thoại không hợp lệ. Vui lòng nhập số điện thoại Việt Nam có độ dài 10 hoặc 11 số và bắt đầu bằng số 0.">
    </div>
    <div class="form-group">
      <label for="address">Địa chỉ:</label>
      <input type="text" id="address" name="address" value="<%= user.address %>" required pattern="^\S.{5,253}\S$"
        title="Địa chỉ phải có độ dài từ 7 đến 255 ký tự.">
    </div>
    <div class="form-group">
      <label for="idCard">CCCD:</label>
      <input type="text" id="idCard" name="idCard" value="<%= user.idCard %>" required pattern="[0-9]{9}|[0-9]{12}"
        title="CCCD/CMND phải có 9 hoặc 12 chữ số.">
    </div>
    <p><strong>ID tài khoản:</strong>
      <%= user.idAccount %>
    </p>




    <div class="form-group">
      <button type="submit">Lưu</button>
      <button type="button" onclick="restoreValues()">Phục hồi</button>
    </div>
  </form>




  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script>
    function editUser(userId) {
      window.location.href = '/user/edit/' + userId + '?url=/user/listUser';
    }
    function restoreValues() {

      var name = document.getElementById("name");
      var email = document.getElementById("email");
      var phone = document.getElementById("phone");
      var address = document.getElementById("address");
      var idCard = document.getElementById("idCard");
      // Khôi phục giá trị ban đầu

      name.value = "<%= user.name %>";
      email.value = "<%= user.mail %>";
      phone.value = "<%= user.phone %>";
      address.value = "<%= user.address %>";
      idCard.value = "<%= user.idCard %>";

    }




  </script>


</body>

</html>