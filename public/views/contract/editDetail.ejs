<!DOCTYPE html>
<html>

<head>
  <title>LIFE</title>
  <link rel="icon" href="../../icon/2764.png" type="image/png">
  <style>
    input {

      border: 1px solid #ccc;
      border-radius: 4px;
    }

    select {
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    select {
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    .container {
      border-radius: 10px;
    }

    h3 {
      font-size: smaller;
      opacity: 0.5;
    }

    .form-group {
      margin: 10px;
    }

    #idStaff {
      outline: none;
      border: none;

    }

    button {
      display: inline-block;
      padding: 5px;
      background-color: #3f87a6;
      border: none;
      color: #fff;
      font-weight: bold;

      border-radius: 4px;
      text-decoration: none;
      cursor: pointer;
    }
  </style>

</head>

<body>



  <h2>Form sửa thông tin chi tiết bảo hiểm</h2>
  <form action="/contract/editDetail/<%=detail_contract.idDetail_contract%>?url=/contract/listDetail" method="post">
    <p><strong>ID Hợp đồng:</strong>
      <%= detail_contract.idContract%>
    </p>
    <p><strong>ID Chi tiết hợp đồng:</strong>
      <%= detail_contract.idDetail_contract %>
    </p>
   
    <div class="form-group">
      <label for="total">Trạng thái:</label>
      <select id="status" name="status" required>

        <option value="0" <%=detail_contract.status==0 ? "selected" : "" %>>
          Đã huỷ
        </option>
        <option value="1" <%=detail_contract.status==1 ? "selected" : "" %>>
          Còn hợp đồng, đang thu phí.
        </option>
        <option value="2" <%=detail_contract.status==2 ? "selected" : "" %>>
          Còn hợp đồng, dừng thu phí.
        </option>
        <option value="3" <%=detail_contract.status==3 ? "selected" : "" %>>
          Hết hợp đồng
        </option>
      </select>
    </div>
    
    <div class="form-group">
      <button type="submit">Lưu</button>
      <button type="button" onclick="restoreValues()">Phục hồi</button>
    </div>
  </form>




  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script>
    function restoreValues() {

      var status = document.getElementById("status");



      // Khôi phục giá trị ban đầu

      status.value = "<%= detail_contract.status %>";
   
     


    }
  </script>


</body>

</html>