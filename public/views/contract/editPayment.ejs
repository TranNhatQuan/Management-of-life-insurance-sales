<!DOCTYPE html>
<html>

<head>
  <title>LIFE</title>
  <link rel="icon" href="../../icon/2764.png" type="image/png">
  <style>
    input {

      border: 1px solid #ccc;
      border-radius: 4px;
    }

    select {
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    select {
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    .container {
      border-radius: 10px;
    }

    h3 {
      font-size: smaller;
      opacity: 0.5;
    }

    .form-group {
      margin: 10px;
    }

    #idStaff {
      outline: none;
      border: none;

    }

    button {
      display: inline-block;
      padding: 5px;
      background-color: #3f87a6;
      border: none;
      color: #fff;
      font-weight: bold;

      border-radius: 4px;
      text-decoration: none;
      cursor: pointer;
    }
  </style>

</head>

<body>



  <h2>Form sửa thông tin lịch thu phí</h2>
  <form action="/contract/editPayment/<%=payment_schedule.idPayment_schedule%>?url=/contract/listPayment" method="post">
    <p><strong>ID Lịch thu phí:</strong>
      <%=payment_schedule.idPayment_schedule%>
    </p>
    <p><strong>ID Khách hàng:</strong>
      <%=payment_schedule.idUser%>
    </p>
    <p><strong>ID Nhân viên thu:</strong>
      <%=payment_schedule.idStaff%>
    </p>
    <p><strong>ID Chi tiết hợp đồng:</strong>
      <%=payment_schedule.idDetail_contract%>
    </p>
    <p><strong>Số tiền:</strong>
      <%=payment_schedule.total%>
    </p>
    <p><strong>Kỳ thứ:</strong>
      <%=payment_schedule.index%>
    </p>

    <div class="form-group">
      <label for="total">Trạng thái:</label>
      <select id="status" name="status" required>

        <option value="0" <%=payment_schedule.status===0 ? "selected" : "" %>>
          Đã huỷ
        </option>
        <option value="1" <%=payment_schedule.status===1 ? "selected" : "" %>>
          Đã thu
        </option>

        <option value="2" <%=payment_schedule.status===2 ? "selected" : "" %>>
          Chưa thu
        </option>
        <option value="3" <%=payment_schedule.status===3 ? "selected" : "" %>>
          Đang thu
        </option>
        <option value="4" <%=payment_schedule.status===4 ? "selected" : "" %>>
          Phạt lần 1
        </option>
        <option value="5" <%=payment_schedule.status===5 ? "selected" : "" %>>
          Phạt lần 2
        </option>
        <option value="6" <%=payment_schedule.status===6 ? "selected" : "" %>>
          Phạt lần 3
        </option>
        <option value="7" <%=payment_schedule.status===7 ? "selected" : "" %>>
          Hết hạn
        </option>
      </select>
    </div>
    <div class="form-group">
      <button type="submit">Lưu</button>
      <button type="button" onclick="restoreValues()">Phục hồi</button>
    </div>
  </form>




  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script>
    function restoreValues() {
      var index = document.getElementById("index");

      index.value = "<%= payment_schedule.index %>";

      var status = document.getElementById("status");


      status.value = "<%= payment_schedule.status %>";
    }
  </script>


</body>

</html>